.PHONY: all ls sleep clean clear

all: ls sleep clear

ls:
	${MAKE} -C ls ${BIN}/ls

sleep:
	${MAKE} -C sleep ${BIN}/sleep

clear:
	${MAKE} -C clear ${BIN}/clear

clean:
	${MAKE} -C ls clean
	${MAKE} -C sleep clean
	${MAKE} -C clear clean
